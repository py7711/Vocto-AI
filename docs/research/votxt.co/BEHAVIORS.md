# Votxt Dashboard Behaviors

Target: https://www.votxt.co/dashboard
Captured after email/password login on 2026-06-29.

## Global System Update Notice

- Target observed on 2026-07-01 after opening `/upload`: a global modal appears over the logged-in app before upload interactions can proceed.
- Target rechecked on 2026-07-02 after email/password login: `/dashboard` and `/upload` no longer displayed the global system update modal in the active browser session, and the logged-in workflows were usable without dismissing a notice.
- Overlay: fixed full viewport, black `rgba(0,0,0,.4)`, z-50.
- Dialog: `448px` by `348px` at `x=416`, `y=186` in a 1280x720 viewport, white background, `1px solid rgb(226,232,240)` border, `8px` radius, `24px` padding, grid layout, `16px` gap, no visible shadow.
- Title row: `Urgent System Update Notice`, `18px/18px`, semibold, foreground `rgb(2,8,23)`, with a `20px` violet `TriangleAlert` icon and `8px` gap.
- Body copy: `We are rolling out an urgent system update to improve service stability. During this update, all Votxt services will be temporarily unavailable.`, `14px/20px`, muted slate, `6px` top margin.
- Impact block title: `Service impact:`, `14px/20px`, medium, foreground; list is disc style with `20px` left padding and `4px` row gap.
- Impact list items: `Uploads, transcription processing, exports, account management, and billing access will be unavailable`; `Please wait until the update is complete before starting new work`; `We will restore access as soon as the update finishes`.
- Footer: flex row with `Dismiss` at the left and `I understand` at the right. `Dismiss` is `86.2px` by `40px`, white with slate-200 border; `I understand` is `116.1px` by `40px`, violet background, white text. Both use `14px/20px`, medium, `8px 16px`, `6px` radius.
- Close control: top-right `16px` icon button at `x=831`, `y=203`, transparent, 70% opacity.
- Local parity updated on 2026-07-01: dashboard, upload, settings, and standalone transcription detail shells can mount a shared target-style system update notice that can be dismissed for the current browser session.
- Local parity updated on 2026-07-02: the system update notice is now feature-gated behind `NEXT_PUBLIC_SYSTEM_UPDATE_NOTICE_ENABLED=true` and is disabled by default, matching the current target state where logged-in dashboard/upload are not blocked by the notice. The target-style notice UI is retained for future periods when the target shows the maintenance modal again.

## Default Dashboard

- Search input filters by file name.
- Upload Files is a button that navigates to `/upload`, keeping the dashboard sidebar and replacing the main area with the upload workflow.
- Paste Link is a button that keeps the `/dashboard` URL and opens a centered `Media Link Transcription` dialog in place.
- Promotion card `See All Plans` opens a full-screen `Plans & Pricing` overlay with a top-right `Close` button, billing tabs, pricing cards, and FAQ.
- Promotion card timer displays a two-digit `MM:SS:CS` countdown. Target re-measured on 2026-07-01 with samples such as `01:34:95`, `00:52:16`, `00:49:69`, and `00:45:47`; the final segment behaves like centiseconds and can exceed `59`.
- Promotion timer card target metrics at 1280px: `276px` by `136px`, x `939`, y `143`, `padding: 24px`, `borderRadius: 16px`, slate-200 border, `rgba(241, 245, 249, 0.6)` background, no shadow. Label row uses an icon `20px`, `gap: 8px`, text `18px/28px` normal weight, and `margin-bottom: 8px`. Timer digits are a flex row with `gap: 8px`; each number box is `55px` by `50px`, `padding: 8px 12px`, white background, slate-200 border, 8px radius, `24px/32px` mono normal text, and `shadow-sm`.
- Dashboard `See All Plans` overlay target metrics at 1280px: dialog is `1152px` by `720px` at `x=64`, `y=0`, with `24px` padding, 8px radius, slate-200 border, no visible shadow, body overflow hidden, and an inner `1102px` by `670px` scroll container at `x=89`, `y=25`. The overlay defaults to the `Monthly` tab and shows only paid `Basic`, `Standard`, and `Pro` cards; the public/pricing-page `Free` card is not present in this dashboard overlay state.
- Target rechecked on 2026-07-02: full pricing overlay tablist sits around `y=165`, the card row starts around `y=237`, and the Monthly `Subscribe now` CTAs sit around `y=378` at 1280x720.
- Target rechecked on 2026-07-02: dashboard full pricing overlay Monthly/Annual plan CTAs are `button` elements with no `href`; `Subscribe now` stays inside the overlay and starts the subscription checkout flow rather than navigating to `/pricing`.
- Row names navigate to transcription detail pages.
- Row action buttons open a compact menu with `Share`, `Export`, `Rename`, `Move`, and `Delete Transcription`.
- Row `Share` opens a centered dialog titled `Share Transcription` with copy `Anyone with the link can view this transcription`.
- Share dialog disabled state says `Sharing is currently disabled. Enable it to generate a public link.` and shows a full-width `Enable sharing` button plus an icon-only top-right close control; there is no bottom `Close` button in the disabled state.
- Row `Rename` opens a centered dialog titled `Rename File` with placeholder `Enter new filename` and `Cancel` / `Rename` buttons.
- Row Rename target metrics at 1280px: dialog is `425px` by `206px` at `x=427.5`, `y=257`, 1px slate border, 8px radius, no visible shadow, 24px padding, black `rgba(0,0,0,.4)` overlay; input is `375px` by `40px` with `padding: 8px 12px` and focused violet border; footer buttons are right-aligned and the initial `Rename` submit is disabled until the filename changes.
- Row `Export` opens a centered `Export Options` dialog with radio options `Text File (.txt)`, `Word Document (.docx)`, `PDF Document (.pdf)`, `CSV File (.csv)`, `SRT Subtitle (.srt)`, and `VTT Subtitle (.vtt)`, plus `Show speaker names`, `Show timestamps`, and an `Export` button.
- Row Export target metrics at 1280px: dialog is `500px` by `512px` at `x=390`, `y=104`, 1px slate border, 8px radius, no visible shadow, 24px padding; file formats are two-column `220px` by `52px` choices; the footer action is a centered `220px` by `48px` purple `Export` button.
- Row `Move` opens a centered dialog titled `Move to Folder` with copy like `Moving "..." from Uncategorized`, folder choices, and `Cancel` / `Move` buttons.
- Row Move target metrics at 1280px with one available destination folder: dialog is `448px` by `270px` at `x=416`, `y=225`, 1px slate border, 8px radius, no visible shadow, black `rgba(0,0,0,.4)` overlay; destination row is `398px` by `38px`, normal 14px text, and the initial `Move` button is disabled until a destination is selected.
- Row `Delete Transcription` opens a centered confirmation dialog titled `Delete Transcription` with copy `Are you sure you want to delete <title>?`, `Cancel`, red `Delete`, and an icon-only close control. Target metrics at 1280px: dialog is `448px` by `216px` at `x=416`, `y=252`, black `rgba(0,0,0,.4)` overlay, 1px slate border, 8px radius, no visible shadow, title `20px/28px` semibold, description `14px/20px` muted slate, footer buttons `40px` high, and the final `Delete` button is enabled immediately. Do not click the final delete action during target QA.
- Local parity updated on 2026-06-30: dashboard toolbar density matches the target compact dashboard metrics (`All Transcriptions` at 20px/500, 256px by 40px search input, 40px Upload/Paste buttons, and 32px Bulk Actions control) and the promotion card vertical rhythm is tightened toward the target 358px card height.
- Local parity rechecked on 2026-07-02: the default dashboard `Bulk Actions` control now has a stable target-width `131px` by `32px` box; local browser measurement at 1280x720 returned `Upload Files` `137x40`, `Paste Link` `126x40`, and `Bulk Actions` `131x32`.
- Local parity updated on 2026-07-01: dashboard `See All Plans` now opens a body-level portal overlay matching the target `1152x720` centered shell, hidden body overflow, `1102x670` inner scroller, default `Monthly` tab, and paid-only `Basic`/`Standard`/`Pro` card set.
- Target rechecked on 2026-07-02: dashboard full pricing overlay `One-Time` shows a Stripe/crypto helper note, then `Lite` and `Plus` cards at target geometry (`402x806`, y `253`) with `Buy now` buttons (`352x40`, y `394`) that are buttons with no `href`. Target bundle identifies these packs as `lite_20260124` and `plus_20260124` and opens checkout in payment mode.
- Local parity updated and verified on 2026-07-02: dashboard full pricing overlay `One-Time` now uses target `Buy now` button semantics and geometry. The local business flow accepts `pack: "LITE" | "PLUS"` in checkout, creates Stripe `mode=payment` sessions via `STRIPE_PRICE_LITE` / `STRIPE_PRICE_PLUS`, and credits 300/600 minutes through webhook `UsageLedger` adjustments; with local price envs empty, the overlay shows an inline Stripe price configuration error and preserves `/en/dashboard`.
- Local parity updated and verified on 2026-07-02: dashboard full pricing overlay Monthly/Annual `Subscribe now` CTAs render as target-style checkout buttons without `/pricing` hrefs.
- Local parity updated and verified on 2026-07-02: dashboard full pricing overlay spacing now uses the target tablist/card rhythm (`mt-6` before billing tabs, `mt-8` before cards, zero top margin on checkout actions), bringing the monthly card row and CTA y positions back in line with target (`card y=237`, CTA y=378 at 1280x720).
- Local billing return-flow parity updated on 2026-07-02: checkout success/cancel URLs and Stripe billing portal return URLs now use the current request origin instead of `NEXT_PUBLIC_APP_URL`, so local `127.0.0.1:3005` checkout and portal flows return to the active dev server origin rather than the default `localhost:3000`. No real Stripe checkout or portal redirect was completed during verification.
- Local parity updated on 2026-06-30: dashboard transcription table now matches the target unframed desktop table density with a 52px light-slate header, 68px rows, 12-column layout, 40x36 row action buttons, and no desktop `COMPLETED` status text in the name cell.
- Local parity updated on 2026-07-01: upward row action menus are no longer clipped by the desktop table scroller, so `Share`, `Export`, and `Rename` are clickable; row `Share` now opens the target-style `448px` disabled sharing dialog with shadcn header/body spacing, plain muted disabled copy, and a single full-width `Enable sharing` button.
- Local share-link business-flow parity updated on 2026-07-02: owner-facing share URLs returned by request-backed task APIs now use the current request origin instead of `NEXT_PUBLIC_APP_URL`, so local dashboard/detail share flows resolve to the active dev server origin (`http://127.0.0.1:3005/...`) rather than the default `localhost:3000`. No new share link was created during verification; the current QA task list had no enabled share links.
- Local compatibility API share-link parity updated on 2026-07-02: legacy transcription list/detail/create endpoints now pass the current request origin into owner share-link serialization too, covering `/api/transcriptions*` and `/api/tasks/*-transcription` responses that include enabled share links.
- Local parity updated on 2026-07-01: row `Export` now uses the target-style `500x512` dialog with a bordered no-shadow shell, two-column format choices, inline checkbox controls, and centered 220x48 purple action.
- Local parity updated on 2026-07-02: row `Export` dialog overlay now uses target `rgba(0,0,0,.4)` / `bg-black/40` instead of the older local `bg-ink/35` overlay.
- Local parity updated on 2026-07-01: row `Rename` now uses the target-style `425px` bordered no-shadow dialog, black/40 overlay, 16px close icon, focused 40px input with violet border, medium 14px footer buttons, and disabled initial `Rename` submit.
- Local parity updated on 2026-07-01: row `Move` now uses the target-style bordered no-shadow shell, black/40 overlay, normal-weight destination rows, equal-width footer buttons, and no preselected destination so the initial `Move` submit is disabled.
- Local parity updated on 2026-07-01: row `Delete Transcription` now opens the target-style `448x216` in-app confirmation dialog instead of a browser confirm; the final destructive API call only runs from the red `Delete` button.
- Local parity updated on 2026-07-01: dashboard promotion countdown now renders a live target-style `MM:SS:CS` countdown with a fast-changing centisecond segment, replacing the previous day-end `HH:MM:SS` interpretation.
- Local parity updated on 2026-07-01: dashboard promotion timer card now matches the target `276x136` no-shadow shell, 24px padding, 18px label, flex timer row, and 55x50 regular-weight digit boxes.
- Settings `Email Notifications` rechecked on 2026-07-01: target renders three checked standalone `role="switch"` controls for transcription success, quota reset, and product updates with no section-level save button. Local parity updated these switches to persist immediately per account in browser storage while keeping the target default checked state and row geometry.
- Target/local Settings `Email Notifications` rechecked on 2026-07-02 at 1280x720: both pages render the section at `x=620`, `y=0`, `628x424`; the three rows are `578x80` at y `127/223/319`, checked switches are `44x24` at x `1162`, and switch thumbs are `20x20` at x `1184` with the target heavier shadow and `translateX(20px)`.

## Bulk Actions

- Clicking Bulk Actions enters bulk mode.
- Bulk mode text shows the current selected count, for example `0 items selected`.
- Move, Export, and Delete are disabled until at least one row is selected.
- The normal `Bulk Actions` control is replaced by a disabled icon-only action in bulk mode.
- Header and row checkboxes appear only in bulk mode.
- Bulk toolbar target metrics at 1280px: selected count and action controls are transparent 20px-high text controls, `14px/20px`, normal weight, computed `padding: 0 12px`; `Move` is blue, `Export` green, `Delete` red, disabled actions use `opacity: 0.5`, and `Cancel` uses foreground text.
- Target rechecked on 2026-07-02 in zero-selection bulk mode: text-control bounding boxes are wider than the computed padding alone suggests: `Move` `84.53px`, `Export` `91.09px`, `Delete` `90.43px`, and `Cancel` `93.91px`.
- Bulk checkboxes are 16px square button-style controls with purple border, 4px radius, and purple checked fill.
- Bulk `Move` opens the target-style `448px` bordered no-shadow dialog with helper `Moving N selected files`; destination rows are `398px` by `38px`, and `Move` starts disabled until a destination is selected.
- Bulk `Export` opens the target-style `500px` by `512px` bordered no-shadow `Export Options` dialog with helper `Export N selected files`, the same two-column format grid as row export, inline checkbox options, and centered `220px` by `48px` Export button.
- Cancel exits bulk mode and returns to default toolbar controls.
- Local parity updated on 2026-07-01: bulk toolbar, bulk Move dialog, and bulk Export dialog now match the target text-control density, black/40 overlay, bordered no-shadow shells, destination row styling, two-column export format layout, and centered export action.
- Local parity updated and verified at 1280x720 on 2026-07-02: dashboard bulk `Move`, `Export`, `Delete`, and `Cancel` text controls now use wider horizontal padding. Local zero-selection widths are now `82.95px` / `90.16px` / `89.79px` / `92.39px`, bringing them out of the previous narrow `58.95px` / `66.16px` / `65.79px` / `68.39px` range and into the target width family.

## Sidebar

- Folder create and account controls are icon buttons.
- Uncategorized is shown as the default folder.
- Current Plan shows daily and minute quotas with progress bars.
- Current Plan target sidebar card metrics at 1280px: logo area is `42.06px` tall at y `16`, with `20px` margin below; plan card is `267px` by `202.5px` at x `16`, y `78.06`, has `16px` internal padding, `8px` radius, a compact `12px/16px` `Current Plan` label, `25px` Free pill, `10px/15px` quota rows, `6px` progress bars, and a `233px` by `36px` `Upgrade Plan` button. The combined top sidebar block is `288.56px` tall and the nav block starts at y `304.56`.
- Folder header action controls are 32px square icon-only buttons; target create icon sits around `x=207`, `y=364.5`, and the adjacent collapse chevron sits around `x=243`, `y=364.5`.
- The folder create icon opens a centered dialog titled `Create Folder`.
- Folder create and rename dialogs use label `Folder Name`, placeholder `Enter folder name`, helper copy `Maximum 40 characters, current: N/40`, and `Cancel` plus `Create` or `Save`.
- Folder create dialog target metrics at 1280px: black `rgba(0,0,0,.4)` overlay, dialog `425px` by `242px` at `x=427.5`, `y=239`, `24px` padding, `16px` grid gap, slate-200 border, 8px radius, white background, no visible shadow. Title is `Create Folder` at `18px/18px` semibold. Close is a 16px icon at `right:16px`, `top:16px`. Input is `375px` by `40px`, focused violet border with 3px low-opacity violet ring. Footer is right-aligned at `y=416`; `Cancel` and disabled `Create` are 40px high, 14px/20 medium buttons.
- Folder row action menu contains `Rename` and `Delete`.
- Folder list target metrics at 1280px: list starts `16px` below the folder header; `Uncategorized` row is `247px` by `44px` at `x=24`, y `412.56`, with `12px` radius, `12px 12px 12px 28px` padding, and `14px/20` medium text. A regular folder row is `247px` by `52.5px` with `4px` top margin, the name baseline at y `476.81`, and its right action trigger is `28px` square at `x=231`, y `472.56`.
- Folder row action menu target metrics at 1280px: `144px` by `87px`, positioned from the folder row action at about `x=231`, `y=504.5`, with `6px` padding, 12px radius, slate-200 border, white background, no shadow, and two `130px` by `32px` menu items. `Delete` is red.
- Folder `Delete` opens an `alertdialog` titled `Delete Folder`. Target metrics at 1280px: black `rgba(0,0,0,.8)` overlay, dialog `420px` by `350px` at `x=430`, `y=185`, `24px` padding, `24px` grid gap, 8px radius, `border: 1px solid rgba(100,103,242,.05)`, no visible shadow. Text: `You are about to delete folder "<name>".` and `Deleting this folder will also permanently delete all transcriptions inside. This cannot be undone.` Footer actions are `Cancel` and red `Delete`. Do not click final `Delete` during target QA.
- Local parity verified on 2026-06-30: sidebar folder/create hover states and move-dialog selected folder states use a visible light purple background.
- Local parity updated on 2026-07-01: folder header create/collapse controls and folder row action triggers now use target-like 32px icon button geometry instead of 36px controls; the header action group right edge is inset to align the create icon at target `x≈207`.
- Local parity updated on 2026-07-01: Current Plan sidebar card is compressed to target-like `204.5px` height with compact 12px/10px typography and 6px progress bars, bringing the Dashboard/Folders vertical rhythm back to the target top block.
- Target rechecked on 2026-07-02: Current Plan minute quota renders as `42` + muted `/120` + a separate lowercase `min` span; the `min` span uses `text-transform: none`, about `11px/16.5px`, and normal weight while the surrounding quota row remains uppercase `10px/15px`.
- Local parity updated on 2026-07-02: Current Plan quota row labels and value suffix now match the target text serialization (`DAILY`, `MINUTES`, and `42/120min` without a space before `min`) with the `min` suffix isolated from the parent uppercase transform.
- Local parity updated on 2026-07-01: folder list rows now use the target `247px` row width, 44px uncategorized row, 52.5px regular folder row, 28px left text inset, and 28px row action trigger.
- Local parity updated on 2026-07-01: folder create/rename dialogs now use the target `425x242` bordered no-shadow shell, black/40 overlay, 16px close control, focused violet input, and right-aligned medium 40px footer buttons.
- Local parity updated on 2026-07-02: folder row action trigger now starts hidden at `opacity: 0`, appears from the full row hover/focus state, and the `Rename`/`Delete` menu matches the target `144x87` no-shadow shell with 16px icons, 32px rows, a 1px slate separator, and red `Delete`.
- Local parity updated on 2026-07-01: folder row `Delete` now opens the target-style `420x350` alertdialog instead of a browser confirm; deleting a folder now permanently deletes the transcriptions inside it, matching the target warning copy.
- Local parity updated on 2026-07-01: folder row `Delete` alertdialog now exposes `aria-modal="true"` and an `aria-describedby` description container, matching the modal semantics used by target-style destructive dialogs.
- Local parity verified on 2026-06-30: Dashboard `Paste Link` stays on `/en/dashboard` and opens the target-style `Media Link Transcription` dialog; Dashboard `Upload Files` remains a button-style control that navigates to `/en/upload`.
- Account menu items are `Billing`, `Email Support`, `Discord`, `Settings`, compact theme buttons `Light`, `Dark`, `System`, and `Sign out`.
- Account control is a bottom sidebar user card, not a standalone gear: rounded white card with green circular avatar, display name, email, and a right chevron.
- Clicking the right-side 32px chevron button inside the account card expands an inline vertical menu inside the same card. It does not open a floating popover.
- Target account card metrics at 1280px: collapsed card is `267px` by `74px` at `x=16`, `y=630`, with 16px padding, 12px radius, slate-200 border, white background, and small shadow. Expanded card is `267px` by `339px` at `x=16`, `y=365`; inline menu is `233px` wide, starts after a 16px top gap, and uses 8px vertical spacing.
- Account menu rows are 40px tall, full sidebar width, muted gray text, with icons for support, Discord, settings, theme, and sign out.
- Theme row keeps label `Theme` on the left and a compact right segmented icon control around 84px wide; `System` is selected by default in the captured logged-in session.
- Target account menu expanded state remeasured on 2026-07-02 at 1280px: menu row controls are `233px` by `40px` at `x=33`, with `padding: 8px 16px`, `gap: 8px`, 12px radius, `14px/20`, weight `500`, slate-500 text, transparent background, and no shadow. The menu stack is `233px` by `297px`, `margin-top: 16px`, `space-y: 8px`, with `Billing` at y `390`, `Email Support` at y `438`, `Discord` at y `486`, `Settings` at y `534`, and `Sign out` at y `647`. Theme row is also `233px` by `40px`, `padding: 0 16px`, slate-100/20 background, transparent border, and 12px radius; theme icon buttons are `26px` square with selected `System` using white background and `0 1px 2px rgba(0,0,0,.1)` shadow.
- Local parity updated on 2026-07-01: account menu trigger now matches the target right-side 32px chevron control instead of making the whole user card clickable, and the card spans the target sidebar width with `x=16`, 16px padding, 12px radius, slate-200 border, white background, and inline expanded menu.
- Local parity updated on 2026-07-01: the QA account OAuth avatar now uses the same Google profile image URL observed on the target account, so the shared logged-in sidebar account card renders a 40px image avatar instead of local fallback initials.
- Local parity updated on 2026-07-01: the QA sidebar folder data now matches the target captured account (`Uncategorized` plus only the `123` folder). Target rechecked on 2026-07-02 after login: `alx to` appears in the bottom account card as the display name, not as a folder row; the file-folder list still contains only `Uncategorized` and `123`. Account menu iconography now uses the target mail icon, Discord badge image, and `SunMoon` theme icon, and the account email text is normal-weight `12px/16px`.
- Local parity updated on 2026-07-01: account menu row widths, row padding, 12px radii, fixed `233px` menu width, and selected theme-button shadow now match the remeasured target expanded state instead of relying on parent-derived width and Tailwind `rounded-xl` defaults.
- Local parity updated on 2026-07-01: QA seed data now deletes non-target folders for the fixed QA user before inserting `123`, so local resets remain aligned with the captured target account folder set (`Uncategorized` plus `123`) instead of preserving stale folders from earlier runs.
- Target rechecked on 2026-07-02: account menu includes a first `Billing` button. On the captured Free account, clicking `Billing` leaves `/dashboard` unchanged, opens no dialog, and opens no new tab. Local parity updated the global workspace account menu to include the same first-row `Billing` button; Free accounts keep the same no-visible-change behavior, while accounts with a Stripe-backed paid subscription can open the existing local billing portal flow.
- Local parity updated and verified on 2026-07-02: after adding `Billing`, the expanded account menu is shifted upward to preserve the target row positions (`Billing` y `390`, `Email Support` y `438`, `Discord` y `486`, `Settings` y `534`, `Sign out` y `647`) instead of pushing the existing rows down.
- Local shared-sidebar recheck on 2026-07-02 at 1280x720: the same account menu overlay geometry holds on `/dashboard`, `/upload`, and `/settings`; after user state settles, the collapsed account card remains at `alx to` y `649` / email y `669`, and the expanded menu rows remain at `Billing` y `390`, `Email Support` y `438`, `Discord` y `486`, `Settings` y `534`, `Sign out` y `647`.
- Target rechecked on 2026-07-02: account menu `Email Support` is a button that opens an in-app support dialog, not a `mailto:` link. The dialog uses a black/40 overlay and a centered `448px` by `298px` shell at `x=416`, `y=211`, with 24px padding, 16px grid gap, slate-200 border, 8px radius, white background, and no shadow. Dialog grid rows are `98px 62px 56px`.
- Target `Email support` dialog content: title row includes a 20px violet mail icon and `Email support` at `18px/18px` semibold. Description starts at `x=441`, `y=262`; response tiers are a 2-column grid at `y=294`, with an amber 16px crown for `Paid users / within 24 hours` and a muted 16px clock for `Free users / within 48 hours`. Email row is `398px` by `54px` at `x=441`, `y=358`; `hi@votxt.co` is 14px normal-weight selectable text; `Copy` is `89.16px` by `36px` at `x=736.84`, `y=367`; footer `Close` is `70px` by `40px` at `x=768.98`, `y=444`; icon close is 16px at `x=831`, `y=228`.
- Local parity updated on 2026-07-02: account menu `Email Support` now opens the target-style in-app support dialog with the same `448x298` shell, `98/62/56` grid rows, horizontal paid/free tier layout, `hi@votxt.co` copy row, and target Copy/Close button geometry.
- Target rechecked on 2026-07-02: account menu `Discord` is a 233px by 40px `button`, not a link. It has no `href`/`target`, uses the same muted 14px/20 medium row styling as the other account menu rows, and contains a Discord image with alt text `Discord`, so its accessible name is `Discord Discord`. Clicking it leaves the dashboard tab on `/dashboard` and opens a separate browser tab at `https://discord.com/invite/RJTaS28UWU`.
- Local parity updated on 2026-07-02: account menu `Discord` is now a target-style button with `alt="Discord"` icon text and opens `https://discord.com/invite/RJTaS28UWU` in a new tab instead of using the stale direct `<a href="https://discord.gg/votxt">` link.
- Target/local account menu rechecked on 2026-07-02: `Settings` is also a 233px by 40px button in the target account menu, with no `href`/`target`, matching the muted row styling of `Email Support`, `Discord`, and `Sign out`. Local parity updated `Settings` from an anchor to a button that navigates imperatively to `/<locale>/settings`, preserving the business flow while matching target DOM semantics.
- Target theme controls rechecked on 2026-07-02: account menu `Light`, `Dark`, and `System` are 26px icon buttons inside the 40px `Theme` row. Default observed state is `System` selected while the root has `font-sans light`; clicking `Dark` keeps the dashboard URL, changes the root class to `font-sans dark`, changes body background to `rgb(2, 8, 23)`, and moves the active 26px button styling to `Dark`.
- Local parity updated on 2026-07-02: account theme buttons now apply global `light` / `dark` root classes instead of only changing the segmented-control active state. `System` follows `prefers-color-scheme`, and the selected preference is persisted as a best-effort browser setting.
- Local parity updated on 2026-07-02: the active theme icon button now uses a dark background and slate-50 foreground once `Dark` is selected, while retaining the white selected-button treatment in light mode.

## Upload Workflow

- `/upload` uses the dashboard sidebar and breadcrumb `Dashboard / Upload Files`.
- `/upload` uses the same fixed viewport shell as `/dashboard`: outer root `display:flex`, `height:100vh`, `overflow:hidden`; desktop sidebar is 300px wide; right main is `flex:1`, `overflow-y:auto`, and `padding:32px` at desktop.
- Upload dropzone copy is `Drag and drop files here`, followed by `or`.
- Main actions are `Select files from your device`, `Paste Link`, and `Google Drive`.
- A `Recent Files` icon-only circular button appears at the upload header right. Target rechecked on 2026-07-02: the button is `32px` square, violet, white icon, pill radius, subtle `0 1px 2px rgba(0,0,0,.05)` shadow, exposes `aria-label="Recent Files"`, and has no `title` attribute.
- Target `Recent Files` behavior rechecked on 2026-07-01: clicking the upload-page icon keeps the page on `/upload`, opens no dialog/menu/popover, and leaves the button focused with `aria-expanded="false"`. Local parity updated the button to stop navigating to dashboard and expose the same collapsed state.
- Local parity updated on 2026-07-02: upload `Recent Files` no longer adds the extra local `title="Recent Files"` attribute, matching the target accessible-label-only button while preserving the target shadow and geometry.
- Initial upload view does not show transcription settings; those controls appear after a file/link flow needs settings.
- `Paste Link` opens a centered `Media Link Transcription` dialog.
- Paste Link dialog copy: `Paste a media link to transcribe video or audio content.`
- Paste Link dialog shows supported platforms: `YouTube`, `TikTok`, `Instagram`, `Facebook`, `X`, `Many other links`.
- Paste Link input placeholder is `Paste a media link`; the adjacent action button is `Search`.
- Paste Link footer shows `Available minutes:N` and a `Buy More Minutes` button.
- Local parity rechecked on 2026-07-02: upload and dashboard default Paste Link dialogs now use the target 640x376 grid shell with a `px-6 pb-6` body wrapper, 8px platform-chip row gap, input/search row at y `421`, divider footer at y `477`, and centered minutes / `Buy More Minutes` row at y `502`.
- `Buy More Minutes` opens a full-screen `Plans & Pricing` overlay on top of the upload page with `One-Time`, `Monthly`, `Annual Save 40%`, pricing cards, FAQ, and top-right `Close`; the target pricing surface defaults to `Annual`, the billing tablist and card headers match `/pricing` geometry, and FAQ questions are collapsed in a plain divide-y list without an outer rounded border card.
- `Google Drive` starts the Google Drive OAuth flow when no connection exists. Do not authorize during research unless explicitly needed.
- The `Supported Formats & Limits` section lists audio formats `aac, amr, awb, flac, m4a, mka, mp2, mp3, oga, ogg, opus, wav, weba, webm, wma`, video formats `3gp, mkv, mov, mp4, mpg, ts, webm, wmv`, `Maximum 50 files`, and `Maximum file size: 5GB per file`.
- Local parity verified on 2026-06-30: upload default page uses the target-style violet dashed dropzone, 384px row-action column, purple circular `Recent Files`, and white no-border desktop `Supported Formats & Limits` card.
- Local parity updated on 2026-06-30: upload shell now matches the target fixed-height scroll model (`flex h-screen overflow-hidden` root, 300px desktop sidebar, independently scrolling right main with 32px desktop padding).
- Local parity verified on 2026-06-30: upload `Media Link Transcription` dialog is 640px wide with top-right icon close, white purple-border input, white purple-border `Search`, platform chips, and `Buy More Minutes` footer action.
- Local parity updated on 2026-07-01: upload and dashboard `Media Link Transcription` `Buy More Minutes` actions now open the shared pricing overlay with `Annual Save 40%` selected by default, while the dashboard promotion-card `See All Plans` overlay remains defaulted to `Monthly`.
- Local parity updated on 2026-07-01: resolved Paste Link dialogs use target-like collapsed selectors instead of native selects. Collapsed text exposes only `English` and `Off`; `Audio Language` opens a 180px `Popular languages` menu inside the dialog, while `AI Summary` opens a 300px listbox above the trigger with selected-state styling and `PRO` badges for paid templates.
- Local parity updated on 2026-07-01: resolved Paste Link dialogs now use target fixed center positioning instead of grid centering, matching the target tall-dialog overflow behavior at 1280x720. Local verified around `640x818` at `x=320`, `y=-49`, with `Transcribe` at y `602`, download link at y `666.5`, and `Buy More Minutes` at y `723`.
- Local parity updated on 2026-07-01: upload `Google Drive Import` fallback dialog now has proper modal semantics and target-style chrome: black/40 overlay, `640px` max-width bordered no-shadow shell, 24px padding, 16px top-right close icon, semibold title, and medium 40px action buttons. The primary disconnected path still attempts OAuth directly, matching the target Google Drive flow.
- Local parity updated on 2026-07-02: upload breadcrumb typography now matches the target normal-weight row with a violet `Dashboard` control/home icon and muted current `Upload Files` segment.
- Target/current mobile upload rechecked on 2026-07-02 at `390x844`: the current target dropzone divider renders uppercase `OR` in the 16px divider row between the drag card and action buttons. Local parity updated the divider text back to uppercase `OR` while preserving the target row geometry.
- Local parity updated and verified at 1280x720 on 2026-07-02: upload breadcrumb now uses target-like `nav aria-label="Breadcrumb"` semantics with a `Dashboard` button, muted slate separator, and no extra hidden duplicate `h1 Upload Files`; local nav y `50` and `Recent Files` y `44` match the target header rhythm.
- Local upload breadcrumb rechecked on 2026-07-02 at 1280x720: the row uses target `12px` flex gap, `Dashboard` measures about `92px` by `20px`, `Upload Files` starts at `x=462`, and the `Recent Files` button remains `32px` square at `x=1206`, `y=44` with no `title` attribute.
- Target/current mobile upload rechecked on 2026-07-02 at `390x844`: `Dashboard` is at `x=32`, `y=30`, Recent Files at `x=326`, `y=24`, drag card at `x=58`, `y=110`, `Select files from your device` at `y=304`, `Paste Link` at `y=376`, and hidden Google Drive remains display-none. Local parity tightened the mobile header-to-upload gap so these y positions align.
- Target/current mobile `Supported Formats & Limits` is a collapsed no-card accordion row at `x=32`, `y=490`, `w=326`, with a `56px` trigger, `16px/24px` medium text, `16px 8px` padding, and `1px solid rgb(226,232,240)` bottom border. Local parity replaced the previous rounded/shadowed mobile details card with this target-like row.

## Transcription Detail

- Detail header has a back button, source/open icon when available, a dedicated share icon, a `More actions` menu, and an `Export` button.
- Detail header `More actions` menu contains only `Rename`, `Move`, and `Delete Transcription`.
- Detail share icon opens the same `Share Transcription` dialog as the dashboard row `Share` action. Enabled-state `Disable sharing` opens an in-app confirmation and only sends the share-link DELETE request from the red `Disable` action.
- Target and local rechecked at 1280x720 on 2026-07-02: the detail Share Transcription disabled dialog is `448px` by `228px` at `x=416`, `y=246`, with `rgba(0,0,0,.4)` fixed overlay, `20px/28px` semibold `Share Transcription` title in `rgb(2,8,23)` with `-0.5px` letter spacing, muted description, disabled copy `Sharing is currently disabled. Enable it to generate a public link.`, and one full-width `Enable sharing` button. Do not click the enable action during read-only target QA.
- Detail `Export` opens a target-style `500px` by `512px` `Export Options` dialog with a bordered no-shadow shell, two-column `220px` by `52px` format choices, inline checkbox options, and a centered `220px` by `48px` purple action.
- Detail `Delete Transcription` opens the same target-style `448px` by `216px` confirmation dialog as dashboard row delete, with title `Delete Transcription`, the current transcription title in the question, `Cancel`, red `Delete`, and a top-right close control.
- Detail page is a full-screen workbench without the dashboard sidebar.
- The top sticky bar is compact: back icon at far left, a single-line truncated title, source/share/more icon actions, and a purple `Export` button at far right.
- Target detail header metrics captured at 1280px: title is constrained to `600px` from `x=84`; source/share/more icon buttons are `40px` square with `12px` radius; Export is about `108px` by `40px` with `12px` radius. The detail More menu is a `260px` wide role-menu at `x=867.5`, `y=56`, with `6px` padding, 12px radius, slate-200 border, no shadow, normal `14px/20` menu items, and a 1px separator above the red `Delete Transcription` item.
- Main detail content starts immediately below the top bar with a left `Transcript` column and a right insight column using tabs `Summary` and `Mind Map`.
- Completed transcript rows render as a read-first timestamped list: muted timestamp on the left and paragraph text on the right, without a visible textarea/editor chrome.
- The transcript toolbar is icon-only: search, segment/order action, copy, translation/language action, and speaker-recognition details.
- The transcript quality rating appears directly under the `Transcript` title in a bordered row with five outline stars. Target rechecked on 2026-07-02: the outer shell is `745.59px` by `38px` at `x=24`, `y=140`, with white background, slate-200 border, 4px radius, and `8px 12px` padding; the inner line is `20px` high with `14px/20px` muted text.
- Target mobile detail rechecked on 2026-07-02 at `390x844`: the transcript toolbar is constrained to a `~210px` left row and its icon group starts at `x≈95 y=80`; the speaker-recognition note is `260x84.5` at `x≈27 y=120`; the mobile rating card is visible at `x=16 y=136 w≈210 h=62`, uses a `12px/16px` label, `20px` stars at `y=169`, and a `28px` close control at `x≈189 y=153`; the first transcript timestamp starts at `x≈32 y=230`.
- When speaker recognition was not enabled, the speaker icon shows a floating note: `Speaker recognition wasn’t enabled when this transcript was created.` with a `Details` link and close affordance. Target rechecked on 2026-07-02: the popover is `260px` by `84.5px` at `x≈509.6`, `y=120`, with slate-200 border, `shadow-lg`, `12px/19.5px` muted copy, and a `12px/16px` medium `Details` control.
- The right insight column includes a `General` selector, regenerate icon, copy icon, and then summary section headings such as `概述`, `要点`, and `启示`.
- A purple upgrade banner plus media controls are fixed to the bottom of the viewport.
- Target rechecked on 2026-07-02: detail bottom upgrade banner `Upgrade Now` is a white pill `button`, not an anchor. It measures about `111.72px` by `32px`, keeps `/transcriptions/...` in the URL, and opens the full `Plans & Pricing` overlay (`1152px` by `720px` at 1280x720) with Annual selected.
- Local parity updated on 2026-06-30: detail page now uses the target full-viewport workbench model (`100dvh`, 64px top bar, independently scrolling transcript/insight panes), target-sized 32px transcript toolbar controls, 40px rating row, left-pane purple upgrade banner, and target-style media bar with 15-second skip controls.
- Local parity updated and verified on 2026-07-02: detail bottom upgrade banner `Upgrade Now` now renders as a target-style button instead of linking to `/pricing`, and opens the shared full `Plans & Pricing` overlay in Annual mode while preserving the current detail URL.
- Local parity updated on 2026-07-01: detail header title/action spacing and the More menu now match the target 600px title constraint, 12px header action radii, 260px no-shadow menu, normal-weight menu rows, and separator before `Delete Transcription`.
- Local parity updated and verified at 1280x720 on 2026-07-02: standalone detail rating row now uses the target `38px` shell and `14px/20px` inner line. The speaker-recognition note now matches the target `260px` by `84.5px` popover at `y=120`, slate-200 border, `shadow-lg`, `12px/19.5px` muted copy, and `12px/16px` purple `Details` control.
- Local parity updated and verified at `390x844` on 2026-07-02: standalone detail mobile toolbar/rating now follows the target narrow left-column rhythm. Local measured toolbar icon start `x≈93.5` versus target `x≈95.2`, speaker note `260x84.5` at `y=120`, rating card `210x62` at `x=16 y=136`, stars `20px` at `y=169`, and first timestamp `x=32 y=230`; desktop rating remains `746x38` with the mobile close control hidden.
- Local parity updated on 2026-07-01: detail Export dialog now matches the target 500x512 shell, two-column 220x52 format grid, inline options, 12px-radius centered Export action, and no-shadow bordered modal treatment.
- Local parity updated on 2026-07-02: standalone detail `Export Options` file-format choices now use visible target-style 16px radio indicators plus text instead of local bordered selection cards.
- Local parity updated on 2026-07-01: detail Rename dialog now matches the target 425x206 shell, input position/size, focused violet ring, 16px close control, medium 14px footer buttons, and disabled initial `Rename` submit until the title changes.
- Local parity updated on 2026-07-01: detail Move dialog now uses the target bordered no-shadow shell, black/40 overlay, 16px close control, normal-weight destination rows, medium equal-width footer buttons, and no preselected destination so `Move` starts disabled; local height can exceed the target empty-state height when the account has destination folders.
- Local parity updated on 2026-07-01: detail `Delete Transcription` now opens the target-style `448x216` in-app confirmation dialog instead of a browser confirm, and does not call the destructive endpoint until the dialog's red `Delete` action is clicked.
- Local parity updated on 2026-07-01: dashboard bulk row selection controls now use target-style 16px `role=checkbox` buttons, and bulk `Delete` opens the target-style `420x330` `Delete Selected Files` alert dialog with black/80 overlay, warning icon, red irreversible-action alert copy, `Cancel`, and red `Delete Files`. The destructive batch delete request is only sent from `Delete Files`.
- Local parity updated on 2026-07-01: shared `Share Transcription` enabled state now uses a target-style in-app `Disable Sharing` confirmation instead of browser `window.confirm`, covering dashboard row share, workspace detail export/share, and standalone detail share. The share-link DELETE request is gated behind the confirmation dialog's red `Disable` action.
- Local parity updated on 2026-07-01: workspace `Delete original file`, `Cancel transcription`, and bulk `Delete Originals` no longer use browser `window.confirm`; they open in-app `448x216` confirmations and gate the final request behind the red action button.
- Local QA data parity updated on 2026-07-02: the SQL QA reset now disables enabled historical share links for canonical task `cmqyzscrp00021lvpt08gi2oo`, and the current local QA database was aligned by disabling the one enabled historical link. The canonical local detail page now matches the target account's first-open share dialog default (`Sharing is currently disabled...`) while preserving the enabled-state component branch for share-flow tests.
- Local parity updated on 2026-07-01: detail insight toolbar now matches the target button-style `General` selector (`240x36`, 12px radius, pale slate background, no border/shadow, 13px text) and target `40x40` regenerate/copy icon buttons.
- Target detail `Mind Map` tab measured on 2026-07-01: active tabs expose `role=tab`, `aria-selected`, and `data-state`; switching to `Mind Map` hides the `General` selector/regenerate/copy toolbar and shows a small `Mind Map` title plus a transparent markmap-style SVG canvas around `438px` by `500px`. Local parity updated the detail page to use the same tab semantics, hide the Summary toolbar in Mind Map state, and render a target-style SVG mind-map canvas instead of the previous rounded card list.
- Target detail `Summary` rhythm measured on 2026-07-01 at 1280px: tablist begins at `y=64` with tabs at `y=74`, `General` toolbar at `y=130`, and Summary content/`概述` at `y=188`; paragraph/list copy uses target 14px density (`20px` paragraph line-height and about `22.75px` list line-height). Local parity updated the standalone detail insight pane to match these vertical positions and text density.
- Local parity updated on 2026-07-01: the standalone detail Summary now supports target-style timestamp references on bullet and insight rows. The QA intro seed summary and AI summary generation schema were updated to structured `{text, timestamps}` entries, rendered as small purple timestamp chips while preserving legacy string-array summaries.
- Local parity updated on 2026-07-01: the dashboard-embedded task detail Summary now uses the same structured summary entry renderer, so `{text, timestamps}` bullets display readable copy plus purple timestamp chips instead of object text.
- Local parity updated on 2026-07-01: standalone and dashboard-embedded task detail now merge realtime/polling task snapshots with the currently loaded detail record, so partial status updates cannot wipe loaded `insights` and make Summary temporarily render as empty.
- Local parity updated on 2026-07-01: detail transcript paragraph text now matches the target `14px/22.75px` read-first list density instead of the previous local `16px/24px` paragraph styling.
- Local parity updated on 2026-07-01: detail transcript rows now include the target inner transparent-bordered text card around paragraph content, restoring the target paragraph width and vertical rhythm.
- Local parity updated on 2026-07-01: detail transcript timestamp rows now use the target nested structure: a `19.5px`-high flex row with `12px` bottom gap wrapping a `13px/19.5px` muted timestamp control, instead of relying on the button/span itself as the row.
- Local parity updated on 2026-07-02: dashboard-embedded detail `重新转写设置` modal now uses the target modal-family black/40 overlay instead of the older local `bg-ink/35` overlay.

## Settings

- Settings uses the same dashboard sidebar plus a settings-specific left navigation.
- Settings does not use the public marketing header; the left dashboard sidebar remains visible with Current Plan, Dashboard, Folders, and account controls.
- Settings uses the same fixed viewport shell as dashboard/upload: outer root `display:flex`, `height:100vh`, `overflow:hidden`; desktop sidebar is 300px wide; right main is `flex:1`, `overflow-y:auto`, and `padding:32px` at desktop.
- Settings header is in the main content column above the settings nav/content split, not inside the settings nav. The current target header includes a 40px square icon-only `Back` button at x `332px`, y `40px`; the title starts around x `388px`, y `32px`, uses `24px/32px` bold text, and the subtitle sits at y `64px`.
- Settings nav/content split starts at x `332px`, y `120px`: left settings nav is `256px` wide, right content column is `628px` wide, with a `32px` horizontal gap.
- Settings tabs: `Profile`, `Account Security`, `Usage`, `Preferences`, `API Keys`, `Notifications`, `Integrations`, `Danger Zone`.
- The settings navigation is a plain vertical button list on the white page, not a framed card; active `Profile` is a solid purple `256px` by `40px` button with white text, while `Danger Zone` uses red text when inactive.
- Superseded local note from 2026-06-30: an earlier local settings navigation used purple text with a light purple background; current local parity uses the target solid-purple active state documented above.
- Local parity updated on 2026-06-30: settings shell now matches the target fixed-height scroll model (`flex h-screen overflow-hidden` root, 300px desktop sidebar, independently scrolling right main with 32px desktop padding).
- Local parity updated on 2026-06-30: settings header/nav geometry now matches the target top layout: icon-only 40px Back control at x/y `332/40`, header at x/y `388/32`, nav/content split at `332/120`, 256px nav, 628px card column, and solid-purple active nav state.
- Target/local mobile Settings Profile rechecked on 2026-07-02 at `390x844`: target hides both the dashboard sidebar and settings section nav; the header wrapper is `358px` wide at `x=16`, `y=16`, Back is vertically centered at `x=16`, `y=36`, about `34px` by `40px`, and the title starts at `x≈66.4`, `y=16`. Local parity updated the mobile header Back sizing/alignment to match while preserving the verified desktop `40px` Back control and desktop nav/card geometry.
- Target/local mobile Settings hash entry rechecked on 2026-07-02 at `390x844`: direct `/settings#security` scrolls the internal settings pane to the Account Security card, placing the `Account Security` title at `x=41`, `y=25` and leaving the preceding `Settings` header above the viewport. Local parity updated normal settings hashes (`#profile`, `#security`, `#usage`, `#preferences`, `#api`, `#notifications`, `#integrations`, `#danger`) to scroll their matching card into view, instead of only updating the active tab state.
- Settings content is rendered as a continuous stack of large cards. Each card has a light gray title header, a thin divider, and a white content body.
- Settings card titles render as semantic `h3` elements with `20px/28px` semibold text; this applies to cards such as `Profile`, `Email Notifications`, and `Integrations`.
- Profile shows a large circular 80px account avatar image when the account exposes an avatar URL, the display name beside it, two fields `First Name` and `Last Name`, and a right-aligned purple `Save` button. The captured target QA account renders the Google avatar at `x=645`, `y=247`.
- Profile card target metrics: card is `628px` wide at x `620px`, header uses `24px` padding with `rgba(241,245,249,.5)` background, body uses `24px` padding with no top divider, inputs are `277px` by `40px` with slate-200 borders and `14px/20` normal-weight text, and `Save` is a right-aligned 40px purple button.
- Local parity updated on 2026-06-30: Profile form controls now match target input/button weight, height, border, padding, and right-aligned Save treatment; the extra local body divider was removed.
- Local parity updated on 2026-07-01: Settings Profile now renders the same OAuth Google avatar URL as the target QA account at 80px instead of a green initials fallback, while retaining initials fallback only when no avatar URL exists.
- Local parity updated on 2026-07-01: shared settings card titles now use `h3` semantics while preserving the target `20px/28px` semibold visual treatment.
- Preferences time zone menu target measured on 2026-07-01: the `Asia/Hong Kong` trigger opens a right-aligned `280px` by `300px` white menu with `4px` padding, slate-200 border, 6px radius, no shadow, a top `254px` by `40px` search input, and `270px` by `32px` timezone rows. Local parity updated the time zone menu to include the same search input and live filtering while preserving the target shell and row metrics.
- Logged-in Account Security shows `Sign-in methods` and copy `Google email is managed by Google. Email login address is managed here.`
- Sign-in methods are grouped inside an inner bordered card. Each method row is a smaller bordered white row with an icon, method name, status text (`Password not set`, `Linked`), and email address.
- Account Security sign-in method cards show `Email + Password`, status `Password not set`, the email address, plus `Google Sign-In`, status `Linked`, and the Google email; current target renders status as a right-aligned pill badge.
- Account Security email form title is `Change Email Login Address`; helper copy starts `Current email login address:`.
- Email input placeholder is `Enter a new email login address`; action button is `Update Email` and remains enabled-looking even when the input is empty.
- Password form title is `Set or Change Password`; helper copy is `Set a password to complete email login setup.` and `Email identity is linked, but password is not set yet.`
- The password identity copy is inside an amber notice block with an amber border/background, not plain muted body text.
- Password fields are `New Password` and `Confirm Password`; action button is `Set Password` and remains enabled-looking before both fields are filled.
- Local parity updated on 2026-06-30: Account Security now matches target method container structure, right-aligned `Password not set` / `Linked` badges, shadcn-style action buttons, slate section dividers, and amber password setup notice.
- Local parity updated on 2026-07-01: Account Security sign-in method rows now use the target fixed `80px` row height, aligning the `Email + Password` and `Google Sign-In` cards with the measured method container rhythm.
- Local parity updated on 2026-07-01: Account Security `Sign-in methods` heading uses target `14px/20px` medium typography, and the `Update Email` / `Set Password` actions are text-only 40px buttons, restoring the target email input/button widths.
- Target rechecked on 2026-07-02: Account Security email/password sub-section titles are `h3.font-medium` headings inside icon rows, and password field labels are standalone inline labels with separate inputs below them. Local parity updated the heading semantics and password label/input DOM structure while keeping the same safe no-submit validation behavior.
- Local Account Security email-change business flow updated and verified on 2026-07-02: `/api/account/profile` now clears `emailVerifiedAt`, creates/sends a fresh verification token for the new email login address, and returns a development `Open verification link` when email delivery is not configured. Verification links use the current request origin, so local settings email-change links resolve to `http://127.0.0.1:3005/...`.
- Local parity updated on 2026-07-02: the settings route now seeds the client settings page with the server current user and preserves that user if the client auth refresh is unavailable, so a hard reload of `/settings#security` remains in the logged-in `Email + Password` / `Google Sign-In` branch instead of the transient `No sign-in methods detected` branch.
- A transient guest/no-method settings state was observed once with `CURRENT PLAN GUEST`, `Signup to Save History`, `No sign-in methods detected. Please refresh this page.`, and an `Enable Email Login` form with placeholder `Enter an email address for login`. Re-login on 2026-06-30 restored the expected Free logged-in account state for `gxx961208@gmail.com`.
- API Keys shows a `Beta` badge and `View API Documentation`; the documentation link sits inside the muted API card header below the description.
- Target rechecked on 2026-07-02: clicking the Settings `API Keys` nav updates the URL to `/settings#api`; the target API card has `id="api"` and no `id="api-keys"` node.
- Target rechecked on 2026-07-02: Settings API Keys `View API Documentation` is an underlined `button` in the header, not an anchor; activating it navigates to `/docs`.
- Local parity updated and verified at 1280x720 on 2026-07-02: Settings API Keys `View API Documentation` now renders as `button type="button"` and imperatively navigates to local `/docs`, preserving the same header placement, 20px control height, 6px parent top margin, and underlined 14px visual style.
- Free accounts show `API access requires an active subscription or LTD plan`, helper copy `Upgrade your plan to access API management features`, and `Upgrade Plan`; paid/LTD accounts can list, create, copy one-time tokens, rename, reset, and revoke API keys.
- Target rechecked on 2026-07-02: Settings API Keys free empty state includes a `48px` yellow triangle alert icon above the heading. `Upgrade Plan` is a `button`, not an anchor, and opens the same in-place `Plans & Pricing` overlay (`1152x720` at 1280x720) with Annual pricing selected while keeping the URL at `/settings#api`.
- Local parity updated on 2026-07-02: Settings API Keys free empty state now includes the target warning icon, renders `Upgrade Plan` as a button, and opens the shared dashboard `Plans & Pricing` overlay in Annual mode instead of navigating to `/pricing`.
- Local parity updated and verified on 2026-07-02: Settings API Keys now uses the target `#api` hash/`id="api"` anchor, direct `/en/settings#api` entry activates the API Keys nav item, and the API empty-state heading plus Danger row label use the target `rgb(2,8,23)` foreground.
- Local parity updated on 2026-07-01: paid/LTD API key rename now uses an in-app `Rename API Key` dialog instead of browser `window.prompt`, with `Key Name`, `Cancel`, violet `Save`, and disabled save for empty or unchanged names.
- Local parity updated on 2026-07-01: paid/LTD API key reset and revoke flows now use in-app confirmation dialogs instead of browser `window.confirm`, gating token reset/revoke requests behind red `Reset` / `Revoke` actions.
- Local parity verified on 2026-06-30: Usage renders the target-style `Free` plan title/badge, `Used`/`Total` progress card, and ghost purple `Upgrade Now`; API Keys renders the orange `Beta` badge, header-positioned underlined docs link, and centered free-plan upgrade empty state; Danger Zone uses the transparent red-divider header and solid red delete button.
- Target rechecked on 2026-07-02: Settings Usage inner-card plan title (`Free`) is an `h4`, not an `h3`, and uses `14px/20px` medium typography. Local parity updated the heading level, typography, and inner-card vertical rhythm so the usage summary card matches the target `136px` height instead of the previous local `140px`.
- Target rechecked on 2026-07-02: Settings Usage free-account `Upgrade Now` is a `button` with a trailing right-arrow icon, measures about `122.88px` by `28px`, keeps `/settings#usage` in the URL, and opens the full `Plans & Pricing` overlay (`1152px` by `720px` at 1280x720) with Annual selected.
- Local parity updated and verified on 2026-07-02: Settings Usage free-account `Upgrade Now` now uses the target right-arrow button treatment and opens the shared `Plans & Pricing` overlay in Annual mode; paid-account `Manage Billing` still uses the billing portal path.
- Local parity updated and verified on 2026-07-02: Settings Usage no longer flashes stale past reset dates from the server-provided initial user object before the usage API response arrives. The client fallback now rolls local/free subscription reset dates forward by month, matching the usage API behavior; fresh `/en/settings#usage` reload rendered future `Resets on Jul 30, 2026, 01:47 PM` with no `Jul 1, 2026` flash.
- Target bundle rechecked on 2026-07-02: paid/LTD users can be routed to `/settings#usage-addon` from the dashboard sidebar add-minutes path, while free users stay in the upgrade dialog flow. The bundle exposes add-on plan IDs `addon_basic`, `addon_standard`, and `addon_pro` with 500/$10, 1000/$15, and 3000/$20 minute packs.
- Local parity updated on 2026-07-02: dashboard sidebar `Upgrade Plan` now sends non-free plans to `/settings#usage-addon`; Settings Usage activates the Usage tab for that hash and renders add-on cards below the normal usage card. Add-on purchases use Stripe `payment` checkout via `STRIPE_PRICE_ADDON_BASIC`, `STRIPE_PRICE_ADDON_STANDARD`, and `STRIPE_PRICE_ADDON_PRO`, and webhook completion credits the active paid subscription with an idempotent `UsageLedger` adjustment.
- Target/local mobile Settings rechecked on 2026-07-02 at `390x844` from `#usage`: direct target `/settings#usage` and local `/en/settings#usage` now align the visible section stack from Usage through Danger. Section positions/heights are `Usage y=0 h=312`, `Preferences y=344 h=428`, `API y=804 h=498`, `Notifications y=1334 h=556`, `Integrations y=1922 h=370`, and `Danger y=2324 h=257`, all at `x=16`, `w=358`. The local fix adds target-like mobile header description wrapping, `24px` Preferences grid gap, `124px` mobile notification rows, and a `198px` mobile Google Drive disconnected row.
- Preferences language and time-zone controls are menu triggers, not plain static fields. `English` opens a 288px-wide, 50vh-tall locale menu with local-language plus English labels for 20 locales; `Asia/Hong Kong` opens a 280px-wide, 300px-tall full IANA time-zone menu. Both triggers are 40px tall with dropdown chevrons.
- Target Preferences menus measured on 2026-07-01 at 1280px: language menu is `288px` by `360px`, time-zone menu is `280px` by `300px`; both use `4px` padding, 6px radius, slate-200 border, white background, no shadow, `z-index:50`, and normal 14px/20 item text. Locale items are 36px for `English` and 52px when a secondary English label is present; time-zone rows are 32px with `6px 8px` padding.
- Local parity updated on 2026-07-01: Settings Preferences dropdown menus now use the target no-shadow slate-bordered shell, z-50 layering, normal-weight item text, and target-like item padding/row heights for locale and time-zone menus.
- Target/local Settings Preferences rechecked on 2026-07-02 at 1280x720: closed section is `628x328` at `x=620`, `y=0`; language trigger is transparent/no-border (`132.56x40` target, `133x40` local) and time-zone trigger is white with a slate border (`142.4x40` target, `142x40` local). Opening either menu scrolls the section to `y=145`; language menu is `288x360` at y `390`, and time-zone menu is `280x300` at `x=926`, `y=384` with a `254x40` search field and `270x32` rows.
- Target rechecked on 2026-07-02 at `/settings#preferences`: the settings section nav remains sticky while the preferences card is at viewport top; Profile/Usage/Preferences/API Keys/Notifications button y positions are `48/136/180/224/268`, and `Preferences` is the active purple item. Local parity updated so direct `#preferences` entry initializes the same active item, nav clicks update the hash, and nav row spacing is independent of active state.
- Target rechecked on 2026-07-02: the `English` language trigger is transparent with no border, while the `Asia/Hong Kong` time-zone trigger remains white with a slate-200 border. Local parity updated the language trigger only, preserving the time-zone trigger styling.
- Local parity verified on 2026-07-02 at 1280x720: `#preferences` nav positions are `48/136/180/224/268`; language trigger is `133px` by `40px`, transparent/no-border at y `195`; time-zone trigger is `142px` by `40px`, white/slate-bordered at y `195`; opening the language menu by visible coordinate keeps the scroll anchor stable and renders the `288px` by `360px` menu at y `245`.
- Email Notifications rows are transparent bordered rows with 16px padding, 8px radius, and 16px vertical spacing.
- Notification switches are compact 44px by 24px controls with a 2px transparent border; checked state is purple, and they toggle locally on click.
- Local parity verified on 2026-06-30: notification rows and switches match target metrics; first switch toggled `true -> false -> true`.
- Target rechecked on 2026-07-02 at 1280x720: notification rows are `578px` by `80px`, switches are `44px` by `24px`, and the white switch thumb uses the target heavier `shadow-lg` shadow. Local parity updated the notification thumb shadow from `shadow-soft` to `shadow-lg` while keeping the same row and switch geometry.
- Integrations Google Drive card shows `Google Drive`, a `Not Connected` status, helper copy `Import audio and video files directly from your Google Drive.`, and a purple `Connect` button when disconnected.
- Local parity verified on 2026-06-30: disconnected Settings Google Drive card includes the target `Not Connected` status, helper copy, and purple `Connect` action.
- Settings Google Drive `Connect` starts Google OAuth with `scope=https://www.googleapis.com/auth/drive.file`, `access_type=offline`, `include_granted_scopes=true`, `prompt=consent`, and redirect URI under `https://api.votxt.co/auth/google-drive/callback`.
- Local parity updated on 2026-07-01: Google Drive OAuth now generates the target public callback path `/auth/google-drive/callback`; the app rewrites that path to the existing `/api/google-drive/callback` handler so Google returns to the target-compatible URL contract.
- Target rechecked on 2026-07-02: Settings Integrations Google Drive disconnected row is `578px` by `102px` with 16px padding/gap; row title is an `h4`; `Connect` is a `button` measuring about `80.64px` by `36px`, not an anchor.
- Local parity updated and verified at 1280x720 on 2026-07-02: Settings Integrations Google Drive row title now uses `h4`, and disconnected `Connect` now renders as a button that starts the same local OAuth route imperatively. Local remeasure: row `578px` by `102px`, `Connect` `79.77px` by `36px`, preserving the target visual geometry and OAuth URL contract.
- Upload-page Google Drive flow rechecked on 2026-07-02: target `/upload` disconnected state navigates directly to Google OAuth with `drive.file`, offline access, `include_granted_scopes=true`, `prompt=consent`, `response_type=code`, and `redirect_uri=https://api.votxt.co/auth/google-drive/callback`; local missing-config state keeps `/en/upload` open and shows the target-style `Google Drive Import` fallback dialog with `GOOGLE_CLIENT_ID 未配置。`.
- Local upload Google Drive fallback remeasured on 2026-07-02 after logged-in user state settled: dialog is `640px` by `272px` at `x=320`, `y=224` over a black/40 grid overlay, with `24px` padding, slate-200 border, `8px` radius, no visible shadow, and the missing-config error rendered as `14px/20px` coral text.
- Target rechecked on 2026-07-02: upload-page sidebar `Upgrade Plan` keeps `/upload` in the URL and opens the compact `Upgrade Your Plan` dialog, not `/pricing`. Local parity updated and verified on logged-in `/en/upload`: sidebar `Upgrade Plan` now opens the compact prompt in place, and compact `See All Plans` opens the full `Plans & Pricing` overlay with `Annual Save 40%` selected while preserving `/en/upload`.
- Danger Zone delete copy is `Permanently delete your account and all data`.
- Danger Zone `Delete Account` opens an in-app `448px` by `506px` confirmation dialog with red warning title, five consequence rows, a `Type DELETE here` confirmation input, `Cancel`, and a disabled red `Delete` button.
- The final Danger Zone `Delete` action becomes enabled only after the confirmation input exactly equals `DELETE`; do not click the enabled final delete action during parity QA.
- Local parity updated on 2026-07-01: Settings Danger Zone now uses the target in-app delete confirmation dialog instead of a browser `window.confirm`, including the 448x506 shell, 398px input, five consequence rows, and `DELETE` enable gate.
- Local parity updated and verified on 2026-07-02: Settings delete-account confirmation remains `448px` by `506px` at `x=416`, `y=107`, keeps the final `Delete` action disabled by default, and now matches the target `h2` title treatment: red `20px/28px`, bold, with `-0.5px` letter spacing.
- Local parity updated on 2026-07-01: the delete dialog foreground color, intro/input/button text color, and close control now match the target `rgb(2,8,23)` / 16px block close treatment while preserving the disabled destructive action until exact confirmation.

## Pricing

- `/pricing` uses the public marketing header and footer, even when inspected from the logged-in browser context.
- Pricing has `One-Time`, `Monthly`, and `Annual Save 40%` tabs.
- Pricing plan feature lists say `Transcription available in 63 languages`.
- Pricing cards mark `AI translation` plus `Limited AI Insights` / `Enhanced AI Insights` with a small `More information` icon and a `New` badge.
- Paid pricing cards include `API access` as a `/docs` link.
- Annual pricing shows Basic `$6 / month` with `$10` struck through and `($72 / year, billed yearly)`.
- Pricing FAQ is a single centered accordion list with questions collapsed by default and answers revealed on click.
- Directly opening `/pricing?checkout=cancel`, `/pricing?checkout=success`, `/dashboard?checkout=success`, and `/dashboard?checkout=cancel` did not show a visible checkout toast, banner, or modal on 2026-06-30; the pages render their normal pricing/dashboard content.
- Target/local pricing mobile rechecked on 2026-07-02 at `390x844`: `One-Time` tab includes the `stripe-crypto.svg` image (`16x16`, alt/title `Crypto payments powered by Stripe`) and the hero subtitle wraps to four lines (`326x112` at `x=32`, `y=208`), putting the tablist at `y=344` and Free heading at `y=441`. Local parity updated the public pricing tab icon/radius/gap and mobile subtitle spacing while preserving the desktop one-line subtitle and `512x40` tablist at `1280x720`.
- Local public-page landmark parity updated and verified on 2026-07-02: `/en/pricing`, `/en/faq`, and `/en/blog` render the public header, route main, and public footer as `body` siblings instead of nesting header/footer inside `main`. Local `/en/pricing` at `390x844` verified one `body > header`, one `body > main`, one `body > footer`, no `main header`, and no `main footer`, while keeping the previously matched pricing hero/tab/card y positions.
- Local public pricing heading parity updated and verified on 2026-07-02: the `Affordable Pricing` heading now renders as target `H2` rather than local `H1`; local `/en/pricing` at `390x844` has `main h1` count `0`, `main h2` count `2`, and unchanged hero/tab/card geometry.
- Target rechecked on 2026-07-02: logged-in dashboard sidebar `Upgrade Plan` is a button, not an anchor. Activating it keeps the URL at `/dashboard` and opens a compact `Upgrade Your Plan` upsell dialog for Basic subscription selection.
- The compact dashboard upsell has a full-viewport `bg-slate-900/60` blurred backdrop and a 448px rounded card. It shows `SUBSCRIPTION`, `Upgrade to Basic Plan Now`, four Basic feature bullets, selected `Yearly Plan $6/mo Billed yearly ($72/year) 40% OFF`, `Monthly Plan $10/mo Cancel anytime`, a primary `Upgrade Now` button, `See All Plans`, and footer trust copy `Cancel anytime` / `Instant access`.
- Compact upsell `See All Plans` keeps `/dashboard` open and swaps to the full `Plans & Pricing` overlay with annual pricing selected. The final `Upgrade Now` checkout action was intentionally not clicked during parity QA.
- Local parity updated on 2026-07-02: dashboard sidebar `Free` plan badge is static, sidebar `Upgrade Plan` and promotion-card `Upgrade Now` open the compact upsell in place, and compact `See All Plans` hands off to the existing full dashboard pricing overlay with Annual selected.
- Local parity updated and verified on 2026-07-02: dashboard promotion countdown no longer initializes from `Date.now()` during SSR/client hydration. The initial rendered countdown is stable and the live deadline starts after mount, preventing the React hydration mismatch previously logged from `DashboardUpgradeCard`. Fresh local dashboard verification showed no hydration/server-rendered HTML console errors and preserved the target promotion/timer geometry (`916x358` promo card, `276x136` timer card).
- Local parity updated and verified on 2026-07-02: dashboard promotion-card `Upgrade Now` now matches the target `542.4x40` button at `x=365`, `y=281`, including the primary violet background color beneath the target gradient and the same shadow/font weight.
- Local parity updated and verified on 2026-07-02: dashboard promotion-card `See All Plans` now matches the target text-button rhythm. Target was about `85.48x20` at `x=593.46 y=335.5`; local now measures `84.88x20` at `x=593.76 y=335`, with `14px/20px` text and no change to the mobile Upload/Paste/Bulk row positions (`y=567/567/571` at 390px).
- Local parity updated and verified at 1280x720 on 2026-07-02: dashboard transcription table header and first row now align with target (`header x=332 y=566 w=916 h=52`, row `x=332 y=618 w=916 h=68`). The first name cell renders the target 16px violet circle-check icon at `x=356 y=644`, 8px gap, and `14px/20px` title link at `x=380 y=642`; duration/created/folder are muted slate, and the type badge is `24x24`, `bg-slate-100/50`, muted slate, `12px/16px`.

## OpenAPI Docs

- `/docs` uses the public marketing header and footer.
- Hero title is `Votxt OpenAPI Documentation (Beta)`.
- A yellow beta notice says the API is in beta and features/endpoints may change.
- Left sticky table of contents includes top-level and nested anchors such as `Overview`, `Base URL`, `Authentication`, `API Key Authentication`, `Getting Your API Key`, `File Upload Workflow`, upload options, endpoint subsections, `Common Error Codes`, integration examples, and `Support`.
- Authentication section shows `X-API-Key: your_api_key_here` and says API access requires an active subscription or LTD plan.
- File workflow has direct upload and external URL options, with 5GB and 10 hour limits.
- Endpoints section includes detailed request bodies, parameters, and JSON responses for upload URLs, create/list/detail/status transcription endpoints, and YouTube transcription.
- Error handling includes error response shape and common API error codes.
- Target mobile docs at 390px show a collapsed `Table of Contents` disclosure above the H1, with the desktop TOC hidden. The H1 uses `24px/32px`, section H2s use `20px/28px`, and the article remains single-column.
- Local parity updated on 2026-07-01: `/en/docs` mobile now matches the target collapsed TOC disclosure and mobile heading scale.
- Target desktop docs rechecked on 2026-07-02: the TOC is a sticky right rail at `x=992`, `y=104`, `256px` wide. Its card is not viewport-clipped and does not scroll independently; the captured card height is `1902px`. Link rows are `222px` wide with `8px` horizontal padding, `4px` vertical padding, `14px/20px` text, `8px` vertical gaps, and long labels wrap to `48px` or `68px`.
- Local parity updated on 2026-07-02: desktop `/en/docs` TOC now uses the target no-scroll full-height card treatment, target link rhythm, target endpoint capitalization, and includes target TOC entries whose anchors already exist in the local article.
- Target desktop docs TOC contains 47 entries, including duplicate `Python Example` links and cURL/code-comment entries for `Create transcription`, `Check status`, upload URL, upload PUT, file-key transcription, external URL transcription, and result retrieval. Local parity updated on 2026-07-02: `/en/docs` now exposes the same 47-entry TOC and target-style Webhook/Error sub-sections; cURL step entries remain visible as code comments instead of extra visible headings, matching the target page.
- Local parity updated and verified on 2026-07-02: `/en/docs` article body now follows the target legacy OpenAPI copy and DOM structure. Endpoint summary cards were removed, target legacy request/response fields were restored, `Rate Limits` and `Language Support` are target-positioned `h3` headings, the footer is outside `main`, section ids live on headings, multi-line code blocks use the target dark inner block (`rgb(45,45,45)`, 16px padding, 6px radius), and the two docs tables match target cell borders/padding/row heights.
- Local parity updated and verified on 2026-07-02: the docs Authentication `Important` block now uses the target plain paragraph/list treatment instead of the previous local card, public header language details load closed like the target, and targeted docs section offsets were tightened. Browser remeasure at 1280x720: body height `19200px` vs target `19203px` (`-3px`), code block heights all match, table heights match (`815px` and `895px`), and the largest remaining adjacent heading interval delta is `16px`.
- Target public mobile header at 390px shows a 180px-wide logo link on the left and a single 40px hamburger button at x `334`, y about `17`. The collapsed header does not show the language switcher directly. Opening the menu reveals full-width rows for `Features`, `Pricing`, `FAQ`, `Blog`, `English`, `Theme`, and `Dashboard`.
- Local parity updated on 2026-07-01: shared public `SiteHeader` mobile layout now uses the target-like hamburger menu and 180px logo area, moving language/theme/dashboard controls into the mobile menu.
- Target logo assets inspected on 2026-07-01: public header uses `https://cdn.votxt.co/logo.svg` (`1181x276`) plus `logo_darkmode.svg` (`1101x172`) in a light/dark image pair with `width=180`, `height=45`; the light image renders at `180px` by `42.0625px`. Local parity updated the root logo SVG assets and shared `BrandLogo` renderer to use the same pair across public, auth, and workspace logo surfaces.
- Target public footer rechecked on 2026-07-02 from `/pricing`: footer group labels such as `Free Tools` and `Company` are paragraph elements with uppercase styling, not `h2` headings. The footer has a 180px logo, icon-only 24px Twitter/Mail links, only two link groups (`Free Tools`, `Company`), a 501px desktop height, and a 615.06px mobile height at 390px. Local parity updated and verified on `/en/pricing`: the full page `h1/h2` list now contains only target `H2` entries `Affordable Pricing` and `Frequently Asked Questions`, footer labels render as `p` elements with title-case text plus uppercase CSS, `Find Us On`/Product Hunt/Crunchbase were removed from the footer, and desktop/mobile footer internals match the target geometry.

## Auth Secondary Flows

- Target `/auth/signin` rechecked on 2026-07-02: page background is pure white, logo/title/Google/email/password/submit/forgot/signup/terms y positions are `60.97/143.03/191.03/327.03/415.03/471.03/527.03/567.03/603.03` at 1280x720, and `Continue with Google` renders as a `button` that starts OAuth rather than as an anchor.
- Local parity updated and verified on 2026-07-02: sign-in background is white, the shell uses a deterministic relative `top: -12px`, the lower form spacing matches the target label padding and 24px forgot-password row, and local y positions verify at `61/143/191/327/415/471/527/567/603` at 1280x720. The shared auth Google control renders as a button while preserving the existing OAuth URL flow.
- Target `/auth/signup`, `/auth/forgot-password`, and `/auth/reset-password` use the same white, centered `286px` auth shell with no card border/shadow. Signup title is `600` weight with divider copy `Or sign up with email`; Forgot and Reset titles are `700`.
- Local parity updated and verified on 2026-07-02: Signup/Forgot/Reset now use the target shell and foreground color `rgb(2,8,23)`. Local signup recheck at 1280x720 measured logo/title/Google/divider/email/Continue y positions `127/209/257/325/389/445`, title weight `600`, and divider copy `Or sign up with email`, matching the captured target within the documented rounding/offset tolerance.
- Target `/auth/verify-email` no-token state uses a centered `384x480` white card with a 180px logo, 48px violet mail icon, `Verify your email` title, muted inbox instructions, a `Resend verification email` button, a bordered `← Back to Sign In` control, and spam-folder helper text. Local parity updated and verified on 2026-07-02: the no-token local page now renders the same card shell and copy, while token-bearing links still post to `/api/auth/verify-email` and show success/failure inside the target shell.
- Local auth email-link business flow updated and verified on 2026-07-02: `Resend verification email` now calls `/api/auth/resend-verification` for the authenticated user, creates a fresh verification token, and shows the returned development `Open verification link` when email delivery is not configured. Verification and password-reset development links now use the current request origin, so the local `3005` server returns `http://127.0.0.1:3005/...` links instead of `localhost:3000`.
- Target `/auth/reset-password` copy uses `Reset Password`, `Enter your new password below`, `New Password`, `Confirm New Password`, and `Reset Password` inside the shared 286px white auth shell.
- Local parity rechecked on 2026-07-02: the English reset-password subtitle remains aligned with the captured target copy and does not include a trailing period inside the visible text.

## Responsive Notes

- Desktop at about 1454px uses a two-column dashboard in a fixed viewport shell: root is `flex h-screen overflow-hidden`, sidebar is `hidden md:flex h-screen flex-shrink-0 sticky top-0`, and the main content pane is `flex-1 overflow-y-auto p-4 md:p-8`.
- Dashboard, settings, and upload use a desktop sidebar with classes equivalent to `hidden md:flex h-screen sticky top-0`; the sidebar is not part of the mobile flow.
- Main logged-in content switches to a single-column mobile layout below the `md` breakpoint.
- Upload shows `Supported Formats & Limits` as a mobile-only collapsed section (`md:hidden`) and as a desktop-only card (`hidden md:block`).
- Local parity updated on 2026-06-30: dashboard root shell now uses target-style fixed viewport height with an independently scrolling main content pane instead of body-level page scrolling.
- Local parity updated on 2026-06-30: root layout now exports `viewport` with `width: device-width` and `initialScale: 1`; before this fix, a 390px mobile viewport rendered local logged-in pages at a desktop CSS width (`innerWidth=1280`) while the target rendered at `innerWidth=390`.
- Target mobile dashboard inspected on 2026-07-01 at 390px: the desktop sidebar is hidden, the promotion card keeps desktop-like horizontal content overflow instead of stacking the timer into the card height, `All Transcriptions` and the 256px search input share one row, `Upload Files`, `Paste Link`, and `Bulk Actions` share a horizontally overflowing row, and the transcription table remains a dense 720px-wide table with 52px header and 68px rows rather than stacking into cards.
- Local parity updated on 2026-07-01: mobile dashboard now keeps the promotion feature list in two columns, lets the timer block overflow horizontally, aligns title/search and action rows with the target mobile geometry, and uses the same dense 720px table/68px rows on mobile.
- Target/local mobile dashboard rechecked on 2026-07-02 at `390x844`: target promotion card is about `358x446.5` at `x=16`, `y=16`; `All Transcriptions` sits at `y≈516.5`, Upload/Paste/Bulk controls at `y≈566.5/566.5/570.5`, and the table header at `y≈646.5`. Local parity updated the mobile-only promotion-card vertical rhythm; local now measures promotion card `447px`, title `y=517`, toolbar `y=567/567/571`, and table header `y=647`, while desktop remains the verified `916x358` card at 1280px.
- Target upload/dashboard `Media Link Transcription` resolved state inspected on 2026-07-01: after link resolution the dialog replaces the input/search row with a source URL row and `Change link`, shows a 180px thumbnail preview, compact 180x36 `English` and `Off` selectors, 44x24 off switches for `Generate Subtitle` and `Speaker identification`, a `Transcribe` CTA, `Click here to download video`, then minutes/pricing footer. Local parity updated the upload and dashboard link dialogs to match that structure and default link-specific settings to English / subtitle off / speaker off / AI Summary Off.
- Target mobile upload `Paste Link` default dialog rechecked on 2026-07-02 at `390x844`: default dialog is fixed-centered, `390px` wide, `max-width:512px`, square-cornered on mobile, about `479-484px` tall, with a transparent/no-border supported-platforms wrapper, `30px` chips, stacked `340px` input/search controls, and a `38px`/`16px` mobile input. Local parity verified after the final input specificity update: upload dialog measured `390x482` at `x=0`, `y=181`, platform wrapper transparent/no-border, chips at `y=354/392/430`, input `340x38` with `16px` text and `8px 12px` padding, search `340x40`, while preserving the verified desktop `640px` dialog.

## Dashboard Table Action Menu

- Target inspected on 2026-06-30 at 1280px: first row action trigger is `40px` by `36px` with transparent background and `6px` radius.
- Target menu opens above the trigger in the current viewport with `role=menu`, `192px` width, `183px` height, `6px` padding, `12px` radius, white background, slate-200 border, no visible shadow, and z-index `50`.
- Target menu items are `Share`, `Export`, `Rename`, `Move`, separator, and `Delete Transcription`; each item is `178px` by `32px`, `padding: 6px 8px`, `14px/20`, weight `400`, radius `6px`.
- Target separator is a `1px` slate-muted horizontal rule with `margin: 4px -4px`.
- Target delete item uses `rgb(220, 38, 38)` and red-tinted focus/hover treatment.
- Local parity updated on 2026-06-30: dashboard row action menu matches the target upward placement and 192x183 menu/item metrics without changing row height.

## Dashboard Table Footer

- Target inspected on 2026-06-30 at 1280px: `Rows per page10` group is centered below rows, `198.99px` wide by `32px` high, `gap: 8px`, muted slate `14px/20` text.
- Target `10` control is a button with `role=combobox`, `92px` by `32px`, `padding: 8px 12px`, `justify-content: space-between`, white background, slate-200 border, `6px` radius, and foreground `rgb(2, 8, 23)`.
- Target expanded state measured on 2026-07-01 at 1280px: the combobox has `aria-expanded=true` and `data-state=open`; a `role=listbox` opens above the trigger (`data-side=top`, `data-align=start`) as a `128px` by `138px` shell with `min-width: 128px`, slate-200 border, `6px` radius, white background, no shadow, and `z-index:50`. The viewport inside is `126px` by `136px` with `4px` padding and contains `10`, `25`, `50`, `100`; option rows are `118px` by `32px`, `padding: 6px 8px 6px 32px`, `14px/20`, normal weight. The selected `10` option has `aria-selected=true`, `data-state=checked`, slate-100 background, and slate-900 text.
- Local parity updated on 2026-06-30: footer group now uses centered target geometry and a 92px combobox-style trigger instead of a right-aligned native select.
- Local parity updated on 2026-07-01: footer rows-per-page is now an interactive target-style combobox/listbox with `10/25/50/100` options, upward placement, checked-state styling, and client-side visible row limiting instead of a static `10` button.
