import {PricingPage} from "@/components/content-pages";

export const metadata = {
  title: {
    absolute: "Pricing | UniScribe - AI Audio & Video Transcription"
  }
};

export default function PricingRoute() {
  return <PricingPage />;
}
