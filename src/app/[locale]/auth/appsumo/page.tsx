import {AppSumoActivationPage} from "@/components/appsumo-activation-page";

export const metadata = {
  title: {
    absolute: "Activate AppSumo | UniScribe"
  }
};

export default function AppSumoPage() {
  return <AppSumoActivationPage />;
}
