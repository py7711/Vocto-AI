import {ResetPasswordPage} from "@/components/auth-pages";

export const metadata = {
  title: {
    absolute: "Reset Password | UniScribe"
  }
};

export default function ResetPasswordRoute() {
  return <ResetPasswordPage />;
}
